[package]
name = "stellar_wrap_proxy"
version = "0.1.0"
edition = "2021"
resolver = "2"

[dependencies]
soroban-sdk = "1.0.0"

[dev-dependencies]
soroban-sdk = { version = "1.0.0", features = ["testutils"] }
